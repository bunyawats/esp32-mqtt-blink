[package]
name = "esp32-mqtt-blink"
version = "0.1.0"
edition = "2021"

[dependencies]
esp-idf-svc = { version = "0.52", features = ["critical-section"] }
log = "0.4"
anyhow = "1"
toml-cfg = "0.2"

[build-dependencies]
embuild = "0.33"

[profile.release]
opt-level = "s"
